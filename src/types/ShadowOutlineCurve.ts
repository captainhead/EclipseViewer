// A shadow outline curve is an array of 2-tuples in [longitude, latitude] form. This form matches the expected polygon format of GeoJSON.

type ShadowOutlineCurve = [long: number, lat: number][];

export default ShadowOutlineCurve;
